# TASKS.md - AgentHub 开发任务

## 第 1 轮任务（已完成 ✅）

全部完成，见历史记录。

---

## 第 2 轮任务（2026-03-20）

### 已完成
- Agent 模块后端 API ✅
- 分类数据初始化 ✅
- 登录/注册页面 ✅
- Agent 列表页 ✅
- Agent 详情页 ✅

### 优化项
1. **全局导航栏（必须修复）**
   - 创建 `apps/web/src/components/layout/navbar.tsx`
   - 左侧：AgentHub Logo + 首页链接
   - 中间：搜索框（调用搜索 API）
   - 右侧：创建 Agent 按钮 + 通知图标 + 用户头像下拉菜单
   - 验收：登录后显示用户菜单，未登录显示登录/注册按钮；移动端汉堡菜单
   - 样式参考：Linear 导航栏的简洁质感

2. **首页重构（必须修复）**
   - 当前 `/` 页面是空壳，需要重构为社区首页
   - Hero 区域：标语 + 搜索框 + CTA 按钮
   - 精选 Agent 展示（横向滚动卡片，6个）
   - 热门讨论预览（最新 5 条帖子）
   - 分类快捷入口
   - 验收：未登录也能看到，视觉冲击力强，动画流畅（framer-motion）

### 新功能
3. **Agent 创建/编辑页面（核心功能）**
   - 创建：`/agents/new`
   - 编辑：`/agents/:id/edit`
   - 表单字段：名称、Logo URL、一句话描述、详细描述（Markdown 编辑器）、分类、标签（多选）、Demo链接、GitHub链接、文档链接
   - 表单验证（复用 validators 包的 Zod schema）
   - 验收：创建后跳转到 Agent 详情页，编辑后数据正确更新；支持草稿保存

4. **用户个人主页（核心功能）**
   - 路由：`/users/:id`
   - 展示：头像、昵称、Bio、技术栈标签、社交链接
   - 统计面板：发布 Agent 数、帖子数、获赞数、粉丝数
   - Tab 切换：Ta 的 Agents / Ta 的帖子 / Ta 的收藏
   - 验收：数据从 API 获取，空状态有友好提示

5. **用户设置页面（核心功能）**
   - 路由：`/settings`
   - 个人资料编辑：昵称、Bio、头像 URL、社交链接
   - 安全设置：修改密码
   - 验收：修改后立即生效，表单验证完整

6. **Post（帖子）后端 API（核心功能）**
   - `post.service.ts` + `post.routes.ts`
   - CRUD：创建帖子、获取帖子列表（支持分页、按频道筛选、排序）、获取帖子详情、更新、删除
   - 字段：标题、内容（Markdown）、频道ID、标签、类型（普通/提问/分享/投票）
   - 点赞、收藏功能
   - 验收：所有接口有 Zod 验证，JWT 鉴权保护写操作

7. **Channel（频道）后端 API（核心功能）**
   - `channel.service.ts` 完善（当前 routes 存在但 service 缺失）
   - 频道列表、频道详情、帖子计数
   - 验收：前端可获取频道列表用于筛选

8. **讨论区页面（增强体验）**
   - 路由：`/discussions`
   - 左侧：频道列表侧边栏
   - 右侧：帖子列表（标题、作者、时间、点赞、评论数、标签）
   - 顶部：排序（最新/热门/精华）+ 创建帖子按钮
   - 帖子详情页：`/discussions/:id`
   - 验收：Markdown 内容正确渲染，代码块有语法高亮

9. **用户关注功能 API**
   - `user.service.ts` 增加 follow/unfollow 方法
   - 获取关注列表、粉丝列表
   - 验收：不能关注自己，重复关注返回友好提示

10. **全局 Footer 组件**
    - `apps/web/src/components/layout/footer.tsx`
    - 内容：版权信息、快速链接（关于、帮助、API文档、GitHub）、社交媒体
    - 验收：所有页面底部显示，移动端适配

### 优先级排序
1. 🔴 全局导航栏（影响所有页面体验）
2. 🔴 首页重构（用户第一印象）
3. 🔴 Agent 创建/编辑页面（核心功能闭环）
4. 🔴 用户设置页面（基础功能）
5. 🔴 Post API + Channel API（社区核心）
6. 🟡 讨论区页面（社区核心前端）
7. 🟡 用户个人主页（增强体验）
8. 🟡 用户关注功能（增强体验）
9. 🟢 全局 Footer（收尾）
